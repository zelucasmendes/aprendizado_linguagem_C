/*Crie um algoritmo que leia uma medida em centímetros (cm), informe o valor
correspondente em polegadas (P). Utilize a fórmula P=C/2,54.*/

#include <stdio.h>
main()
{float cm,pol;
printf("Digite a medida em centimetros:\n");
scanf("%f",&cm);
 pol=cm/2.54;
printf("A medida em polegadas e %.2f",pol);
}
